
// ---------------------------------------------------------
// POINT D’ENTRÉE DE LA FUTURE CARTE DU MONDE
// ---------------------------------------------------------
document.addEventListener('DOMContentLoaded', function () {
    const zoneCarte = document.querySelector('.zone-carte-simulee');

    if (!zoneCarte) {
        return;
    }

    // La grille visible, la caméra et le déplacement case par case
    // seront branchés ici dans la prochaine phase.
});
