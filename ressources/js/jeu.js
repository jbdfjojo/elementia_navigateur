
// ---------------------------------------------------------
// POINT D’ENTRÉE DE L’INTERFACE DE JEU
// ---------------------------------------------------------
document.addEventListener('DOMContentLoaded', function () {
    const pageJeu = document.querySelector('.page-jeu');

    if (!pageJeu) {
        return;
    }

    // Zone réservée aux futures interactions de l’interface joueur.
});
