
// ---------------------------------------------------------
// POINT D’ENTRÉE DE LA PARTIE CONNEXION / INSCRIPTION
// ---------------------------------------------------------
document.addEventListener('DOMContentLoaded', function () {
    const vueAuthentification = document.querySelector('.page-authentification');

    if (!vueAuthentification) {
        return;
    }

    // Zone réservée aux futures interactions de connexion.
});
